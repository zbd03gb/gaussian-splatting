# Introduction
## MAST3R
**图像匹配**是三维视觉中所有高性能算法与系统的核心问题，但尽管匹配在本质上与相机位姿和场景几何紧密相关、属于一个三维问题，现有方法通常将其作为二维问题来处理。

本文提出**将匹配明确建模为一个三维任务**，基于 DUSt3R 这一 Transformer 驱动的三维重建框架进行扩展，**在点图回归的基础上引入一个新的稠密局部特征输出头，并通过额外的匹配损失进行训练**。

本文在保持 DUSt3R 对极端视角变化高度鲁棒性的同时显著提升了匹配精度，并通过提出一种具有理论保证的快速互易匹配方案，有效缓解了稠密匹配的二次复杂度问题，实现了数量级的加速与性能提升。

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768734248817-f7a3bb67-7f99-4436-ab28-ad9fc4362625.png)



## POW3R
三维视觉回归模型在实际应用中往往可以**获得相机内参、相对位姿或深度**等先验信息，但现有前馈式方法在测试时通常无法有效利用这些已知先验。

本文提出了 Pow3R，一种新的大规模三维视觉回归模型，**能够在单一网络中灵活接收并融合图像与任意组合的辅助信息（如内参、相对位姿、稠密或稀疏深度）**。该方法建立在 DUSt3R 的 Transformer 架构之上，通过轻量且通用的条件化机制，将辅助信息作为额外引导，从而在先验可用时预测更精确的结果。

Pow3R 通过在训练阶段随机提供不同模态子集，使模型能够适应测试时不同程度的先验信息，并由此解锁新的能力，如原生分辨率推理和点云补全；实验表明，其在三维重建、深度补全、多视图深度预测、多视图立体和多视图位姿估计等任务上均取得了最先进的性能。  

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768735112697-cc9fa3fa-5342-4730-a52d-f66ca6e53abc.png)



## 新视角生成
新视角生成旨在从有限数量的已知视角图像出发，学习或重建场景的三维表示，从而在未观测过的相机位姿下合成逼真的新视角图像 。

本次实验选择3dgs渲染获得新视角， 它通过学习一组带有位置、尺度、不透明度与颜色属性的三维高斯来表示场景，并利用高效的可微 splatting 渲染在任意新相机位姿下生成图像 。



# Experiment
本实验旨在使用3r模型输出的图像对的匹配，替换传统colmap的匹配阶段，并构造一个完整的colmap sfm流程。将colmap sfm的输出送入3dgs训练，获取包含3dgs椭球的ply点云文件。最后通过3dgs splatting渲染获取新视角。

### 人脸数据准备
使用nersemble数据集作为3r模型的输入。nersemble数据集提供了16视角的高清人脸采集数据，原始数据以视频的形式保存。本实验选择编号为31的人脸，并截取每个视角下的人脸表情变化视频的第一帧，作为3R模型的输入数据。



### 匹配结果获取
mast3r和pow3r分别新增colmap_match 脚本，从输入的16张图像中构造图像对（120对），调用mast3r和pow3r 模型，在pointmap输出的基础上，使用NN获取匹配点并过滤低置信度匹配点。最终将关键点与匹配保存为可供colmap mapping输入的database.db文件。



### 3dgs数据准备
准备两组数据：

1. 将先前获取的database.db文件，送入colmap mapping 和 undistortion 流程，获取最终 colmap sfm 的输出：通常包括估计的相机内参，相机位姿的二进制文件和稀疏点云ply文件。数据文件夹格式如下：

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768740891423-ef3747f2-4599-4559-a010-7e6e753c04de.png)

2. 原始图像文件夹，包含16视角的人脸图像



### 渲染结果展示

**mast3r**

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768747026596-dda8ca87-e76a-4f9c-bd5e-2f99dafa482d.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768747057198-bc2c7094-3279-4cce-9c95-f62961801d8c.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768747067114-4bd2d35a-5486-4d08-b8a4-ab2a5b2cfd21.png)

**pow3r**

之所以是渲染结果是横向的，是因为pow3r的模型输入要求图像为横向的（宽大于高），所以之后经一系列的输出比如相机内参和3dgs输入的图像也都是经过转置的。

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768742739728-a1246f97-8577-4130-9b12-b85c5fc5a65d.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768742873024-4f45b359-f2b8-4e06-a22c-d7c3193d99b3.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2026/png/58377837/1768742915693-b42e4aa0-2680-43f2-ab18-b88f03e1b4f4.png)















## 








